# Hello Plugin

Este é um plugin mínimo de exemplo para documentar a especificação de extensibilidade e novos desenvolvimentos para o **Hermes Agent Hub**.

Ele demonstra a assinatura do manifesto `plugin.json` e a estrutura do entrypoint `scan.ps1` aderente ao contrato de dados padrão do core.
